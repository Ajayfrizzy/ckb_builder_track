[workspace]
members = [
    "vault-common",
    "inherit-vault-lock",
    "inherit-vault-type",
    "tests",
]
resolver = "2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
